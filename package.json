{
  "scripts": {
    "postinstall": "npm install --prefix front && npm install --prefix PerreraBack",
    "start": "npm run start:back & npm run start:front",
    "start:front": "npm --prefix front start",
    "start:back": "npm --prefix PerreraBack start"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.5.2",
    "react-router-dom": "^5.3.0"
  }
}
